/**
 * Paths relative to the project root for file status tooling (checks, ensure steps).
 * Extend as you add rules (existence, size, mtime, etc.).
 *
 * This base template branch keeps the list empty - it is an intentional fill-in slot. Template
 * branches / forks add the files they want protected from accidental edits (e.g. a generated
 * customization guide kept read-only on disk).
 */

export const readOnlyPaths: readonly string[] = [
];
