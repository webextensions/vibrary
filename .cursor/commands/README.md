# Cursor Commands - Pointer Stub

Project-scoped slash-command workflows live in [.claude/commands/](../../.claude/commands/) as
`cmd-<slug>.md` files (plain Markdown - readable by any tool); reusable cross-project commands live
in the user-global tool config. Mirror project commands here as `.cursor/commands/cmd-<slug>.md`
counterparts when Cursor-specific copies become useful, using the user-global
`cmd-sync-ai-instructions-*` commands.
