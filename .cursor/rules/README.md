# Cursor Rules - Pointer Stub

Cursor reads the root [AGENTS.md](../../AGENTS.md) natively; topical standards live in
[.claude/rules/](../../.claude/rules/) (plain Markdown - readable by any tool), with the decision
charter behind them in [.claude/rules/first-principles.md](../../.claude/rules/first-principles.md).
Mirror them here as `.cursor/rules/<topic>.mdc` counterparts when Cursor-specific copies (with
Cursor's `globs` frontmatter) become useful, using the user-global `cmd-sync-ai-instructions-*`
commands.
