# Codex Playbooks - Pointer Stub

Project-scoped workflows for this repository live in [.claude/commands/](../../.claude/commands/)
as `cmd-<slug>.md` files (plain Markdown - readable by any tool); reusable cross-project commands
live in the user-global tool config. Mirror project commands here as
`.codex/playbooks/cmd-<slug>.md` counterparts when Codex-specific copies become useful, using the
user-global `cmd-sync-ai-instructions-*` commands.
