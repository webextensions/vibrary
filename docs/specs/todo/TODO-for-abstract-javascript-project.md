# TODO - For `abstract-javascript-project` base template

> TODOs for the `abstract-javascript-project` base branch itself (the root of the template family - see [docs/template-project/README.md](../../template-project/README.md)).
>
> Project-specific TODOs live in [TODO.md](./TODO.md).

* Add `eslint-plugin-async-protect` (the `async-protect/async-suffix` + `async-protect/async-await`
  rules backing the Async-suffix convention documented in
  [.claude/rules/function-patterns.md](../../../.claude/rules/function-patterns.md)) once it supports
  ESLint 10 - it currently declares peer `"eslint": "5 - 8"`. There is a matching TODO comment in
  [eslint.config.js](../../../eslint.config.js).
