# Keeping the template link

Applies to: new project only (layer branches live in this repository already).

Don't remove the template git remote/branch - it's how you pull future template updates. See
[docs/template-project/template-sync.md](../../template-project/template-sync.md).
