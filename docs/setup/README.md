# Setup

One-time setup guides (for example: machine and tooling prerequisites, third-party integrations).
Name files as kebab-case actions (`set-up-github-repository.md`), and link to the sources of truth
instead of copying facts from them.
